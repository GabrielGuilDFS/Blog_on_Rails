<%= form_with(model: @post, local: true) do |f| %>
  <% if @post.errors.any? %>
    <div class="errors">
      <h2><%= pluralize(@post.errors.count, "erro") %> proibiu salvar:</h2>
      <ul>
        <% @post.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :Titulo %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :Conteúdo %>
    <%= f.text_area :content %>
  </div>

  <div>
    <%= f.label :Publicado_em %>
    <%= f.datetime_select :published_at %>
  </div>

  <%= f.submit @post.new_record? ? "Criar" : "Atualizar" %>

  <% end %>
